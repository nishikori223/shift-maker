# シフト表自動作成アプリ　システム仕様書（Claude Code 向け）

Version 1.1 ／ 更新日：2026年4月

---

## 1. プロジェクト概要

本アプリは、月次シフト表をブラウザ上で自動生成・編集・出力できる静的Webアプリケーションです。GitHub Pages にデプロイし、社員情報やシフトルールは JSON ファイルで管理します。

| 項目 | 内容 |
|------|------|
| 対象規模 | スタッフ10名以下（社員＋パート混在） |
| シフト単位 | 月次（1ヶ月） |
| シフト種別 | 早番 / 遅番 / 時短早番 / 時短遅番 / 休み |
| デプロイ先 | GitHub Pages（静的ホスティング） |
| データ管理 | JSONファイルをアップロードして設定を反映 |
| 出力形式 | 画面表示（印刷対応） ＋ Excelファイル（.xlsx） |

---

## 2. 技術構成

### 2-1. スタック

| レイヤー | 採用技術 |
|----------|----------|
| フロントエンド | React + TypeScript（Vite ビルド） |
| スタイリング | Tailwind CSS |
| Excel 出力 | SheetJS（xlsx ライブラリ） |
| ホスティング | GitHub Pages（gh-pages ブランチ） |
| データ永続化 | なし（セッション内メモリ ＋ JSONアップロード） |

### 2-2. ファイル構成（主要）

| ファイル / ディレクトリ | 役割 |
|------------------------|------|
| src/App.tsx | アプリルート・ルーティング |
| src/pages/Settings.tsx | 設定画面（スタッフ・シフト種別・ルール入力） |
| src/pages/Requests.tsx | 希望入力画面（希望休・希望シフト） |
| src/pages/Schedule.tsx | シフト表生成・表示・編集画面 |
| src/engine/scheduler.ts | 自動割り当てロジック（コアエンジン） |
| src/utils/excelExport.ts | SheetJS を使った Excel 出力ユーティリティ |
| src/types/index.ts | 共通型定義（Staff, ShiftType, Rule, Schedule 等） |
| public/sample-config.json | 設定JSONのサンプルファイル |

---

## 3. データ定義（JSON スキーマ）

### 3-1. 設定ファイル（config.json）

ユーザーがアプリにアップロードすることで設定を反映するファイル。

#### スタッフ定義（staffs）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| id | string | 一意識別子（例: "s01"） |
| name | string | スタッフ氏名 |
| employmentType | string | "fulltime"（正社員）\| "short"（時短社員）\| "part"（パート） |
| eligibleShifts | string[] | 入れるシフト種別ID一覧 |
| targetDaysOff | number | 月の目標休日数（毎月指定。前後1日程度の誤差許容） |

#### シフト種別定義（shiftTypes）

| id | label | 時間帯 |
|----|-------|--------|
| early | 早番 | 08:00 〜 17:00（正社員） |
| late | 遅番 | 11:00 〜 20:00（正社員） |
| short_early | 時短早番 | 08:00 〜 15:00（時短社員のみ） |
| short_late | 時短遅番 | 13:00 〜 20:00（時短社員のみ） |
| off | 休み | — |

#### config.json サンプル

```json
{
  "staffs": [
    {
      "id": "s01",
      "name": "山田 太郎",
      "employmentType": "fulltime",
      "eligibleShifts": ["early", "late"],
      "targetDaysOff": 9
    },
    {
      "id": "s02",
      "name": "鈴木 花子",
      "employmentType": "short",
      "eligibleShifts": ["short_early", "short_late"],
      "targetDaysOff": 9
    },
    {
      "id": "p01",
      "name": "田中 一郎",
      "employmentType": "part",
      "eligibleShifts": ["early", "late"],
      "targetDaysOff": 0
    }
  ],
  "shiftTypes": [
    { "id": "early",       "label": "早番",     "startTime": "08:00", "endTime": "17:00", "color": "#90EE90" },
    { "id": "late",        "label": "遅番",     "startTime": "11:00", "endTime": "20:00", "color": "#FFD700" },
    { "id": "short_early", "label": "時短早番", "startTime": "08:00", "endTime": "15:00", "color": "#ADD8E6" },
    { "id": "short_late",  "label": "時短遅番", "startTime": "13:00", "endTime": "20:00", "color": "#DDA0DD" },
    { "id": "off",         "label": "休み",     "startTime": null,    "endTime": null,    "color": "#F5F5F5" }
  ]
}
```

---

## 4. 画面構成

### 4-1. 設定画面

- スタッフ情報の追加・編集・削除（雇用区分・目標休日数を含む）
- シフト種別の時刻・色設定
- 割り当てルールの設定
- config.json のインポート（アップロード）／エクスポート（ダウンロード）

### 4-2. 希望入力画面

- 対象月・対象スタッフをドロップダウンで選択
- カレンダー形式で「希望休（最大5日）」「希望シフト種別」を入力
- 希望休が5日を超える場合はエラーで入力を拒否
- パートスタッフは毎月の希望シフトをここで申告（申告なし日は休みとして扱う）
- 入力データは requests.json としてダウンロード・アップロード可能

### 4-3. シフト表生成・表示画面

- 対象年月と各スタッフの目標休日数を入力して「シフト生成」を実行
- 横軸：日付、縦軸：スタッフ名 のグリッド表形式で表示
- 各セルをクリックして手動でシフト種別を変更可能
- シフト種別ごとに色分け表示
- 月計・日計サマリー行・列を表示
- 制約違反がある場合は該当セルを赤枠でハイライトし警告メッセージを表示
- 「Excel 出力」ボタンで .xlsx ファイルをダウンロード
- 印刷ボタンで印刷用レイアウトに切り替えて出力

---

## 5. 自動割り当てロジック

### 5-1. スタッフ区分と勤務パターン

| 区分 | employmentType | 割り当て可能シフト |
|------|---------------|------------------|
| 正社員（通常） | fulltime | 早番（08-17）・遅番（11-20）・休み |
| 時短社員 | short | 時短早番（08-15）・時短遅番（13-20）・休み |
| パート | part | 希望申告した日のみ勤務。シフト種別も申告通り |

### 5-2. 割り当てルール（制約条件）

| # | ルール | 詳細 |
|---|--------|------|
| R1 | 社員1名必須ルール | 全営業日において、社員（fulltime または short）が最低1名以上シフトに入ること。パートのみの日は制約違反 |
| R2 | 2名以上確保ルール | 全営業日において、社員・パートを問わず合計2名以上が勤務していること |
| R3 | 希望休の優先 | スタッフが申告した希望休（最大5日/月）は原則として確定「休み」とする。R1・R2 との矛盾が生じる場合は警告を表示し管理者が手動調整 |
| R4 | 目標休日数の遵守 | 各社員の月間休日数が targetDaysOff の ±1日以内に収まるよう割り当てる |
| R5 | パートの勤務日確定 | パートスタッフは毎月の希望申告日のみ勤務。申告のない日は自動的に「休み」 |
| R6 | 時短社員のシフト制限 | 時短社員には early / late は割り当てず、short_early / short_late のみ割り当てる |
| R7 | 固定休日なし | 曜日・祝日による固定休日は設定しない。休日は目標休日数に基づいて自動分散 |

### 5-3. 割り当てフロー

| Step | 処理 | 詳細 |
|------|------|------|
| 1 | 入力読み込み | config.json・requests.json・対象年月・各スタッフの targetDaysOff を読み込む |
| 2 | パート確定 | パートの申告シフトをそのまま確定する |
| 3 | 希望休確定 | 社員の希望休日（最大5日）を「休み」で仮確定する |
| 4 | 社員割り当て | R1〜R4・R6 を満たすよう、残り日程に早番/遅番/時短を割り当てる |
| 5 | 制約チェック | 全ルールを検証し、違反がある日・スタッフを特定して警告リストを生成 |
| 6 | 結果出力 | シフト表グリッドに表示。警告セルは赤枠でハイライト |

---

## 6. Excel 出力仕様

- ライブラリ：SheetJS（xlsx）をフロントエンドで使用
- シート構成：1シート（シフト表）
- 1行目：日付ヘッダー（1日〜月末）＋ 月間休日数・勤務日数の集計列
- 2行目以降：スタッフごとのシフト種別ラベル（早番 / 遅番 / 時短早番 / 時短遅番 / 休）
- 最終2行：早番人数・遅番人数の日別サマリー（社員のみカウントと全体カウントを分けて表示）
- セル背景色：シフト種別の color を HEX で適用
- 制約違反セルは背景をオレンジ色でマーク
- ファイル名：shift_YYYYMM.xlsx

---

## 7. 非機能要件

| 項目 | 要件 |
|------|------|
| 動作環境 | 最新 Chrome / Edge / Safari（モバイルは考慮しない） |
| サーバー不要 | すべてフロントエンド完結。バックエンド・DB なし |
| データ保護 | 個人情報はブラウザ外に送信しない。JSON はローカル保存のみ |
| パフォーマンス | 10名・31日分のシフト生成を 1秒以内に完了すること |
| 保守性 | シフト種別・ルールを設定画面から変更可能（コード修正不要） |

---

## 8. 開発・デプロイ手順

### 8-1. 初期セットアップ

```bash
npm create vite@latest shift-scheduler -- --template react-ts
cd shift-scheduler
npm install
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
npm install xlsx
npm install -D gh-pages
```

package.json に以下を追加：

```json
{
  "homepage": "https://<GitHubユーザー名>.github.io/shift-scheduler",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
```

### 8-2. GitHub Pages 公開

```bash
git init
git remote add origin https://github.com/<ユーザー名>/shift-scheduler.git
npm run deploy
```

リポジトリ設定 → Pages → Source: gh-pages ブランチ を選択

---

## 9. 確定済みルール詳細

> すべての未確定事項が確定しました。この仕様書をそのまま Claude Code に渡して開発を開始できます。

| # | 項目 | 確定内容 |
|---|------|----------|
| 1 | 固定休日 | なし。曜日・祝日による固定休はない |
| 2 | 正社員の勤務時間 | 早番：08:00〜17:00 / 遅番：11:00〜20:00 |
| 3 | 時短社員の勤務時間 | 時短早番：08:00〜15:00 / 時短遅番：13:00〜20:00 |
| 4 | パートの勤務スタイル | 毎月申告制。固定曜日・時間なし。申告日のみ勤務 |
| 5 | 月間休日数の管理 | 毎月管理者が目標休日数を指定。その数の ±1日以内で調整 |
| 6 | 希望休の上限 | 1人あたり月最大5日まで申告可能 |
| 7 | 社員必須ルール | 全営業日：社員（正社員・時短）が最低1名以上出勤 |
| 8 | 最低出勤人数ルール | 全営業日：社員・パート合計で最低2名以上出勤 |
| 9 | 連勤上限 | 特に制限なし（目標休日数の分散で自然に調整） |
